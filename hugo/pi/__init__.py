"""Pi 5 desk modules — local I/O and rendering.

Runs on the Raspberry Pi 5 inside the enclosure. Handles camera
capture, ToF tap detection, mic input, and PyGame HDMI rendering.
Communicates with the Mac mini inference server over WiFi HTTP.
"""
