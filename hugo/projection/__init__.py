"""Projection module — render overlays, display via AirPlay.

Generates visual overlays (icons, arrows, animations, color cues)
aligned to the physical worksheet. The Mac mini renders frames
and sends them to the Kodak Luma 350 via macOS screen mirroring
(AirPlay). In dev mode, renders to a local window or file.
"""
