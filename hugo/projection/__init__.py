"""Projection module — render and display help frames.

Generates visual overlays (icons, arrows, animations, color cues)
aligned to the physical worksheet. Uses Pygame or Cairo to render
frames to HDMI out. In dev mode, renders to a desktop window.
"""
